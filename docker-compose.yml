services:
  postgres:
    container_name: draftgg_postgres
    image: postgres
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: draftgg-db
      PGDATA: /data/postgres
    volumes:
      - ./docker/postgres/data:/data/postgres
