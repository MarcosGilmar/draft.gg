'use client';

export function AppDrawer() {
  return (
    <aside className="sticky top-0 h-screen w-38 bg-linear-to-r from-accent-muted/50 to-accent-muted">
      <nav className="flex items-center h-full justify-center">navegação</nav>
    </aside>
  );
}
